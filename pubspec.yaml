name: kart_kampanya
description: Kart kampanyalarını bul, kartlarını yönet ve avantajları karşılaştır.
publish_to: "none"
version: 1.0.0+29

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.9.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
