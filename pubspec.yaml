name: dini_vakit
description: Hadis, ayet, dini görseller ve namaz vakitleri uygulaması.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  shared_preferences: ^2.5.3
  timezone: ^0.10.1
  flutter_local_notifications: ^19.4.1
  wallpaper_manager_flutter: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
