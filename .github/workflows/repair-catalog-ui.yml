name: Repair catalog UI

# Disabled: this legacy repair job was overwriting the current catalog/detail build.
# Keep the workflow file for history, but only allow manual runs when explicitly needed.
on:
  workflow_dispatch:

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Legacy repair disabled
        run: echo "Legacy catalog repair is disabled; main.yml is the single source of truth."
